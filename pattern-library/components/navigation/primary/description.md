Formatting and layout of the navigation bar is handled by the pre-written classes in CSS, see code sample for IDs.
Used to navigate around the page and highlights the current location in the document.